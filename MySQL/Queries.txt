CREATE TABLE Employee (
    ->   Emp_id INT PRIMARY KEY,
    ->   Dept_id INT,
    ->   Emp_fname VARCHAR(30),
    ->   Emp_lname VARCHAR(30),
    ->   Emp_Position VARCHAR(30),
    ->   Emp_salary DECIMAL(10,2),
    ->   Emp_JoinDate DATE
    -> );
 INSERT INTO Employee VALUES
    -> (101, 1, 'Amit', 'Sharma', 'Manager', 75000.00, '2020-06-15');

select * from Employee e join Dept d on e.Dept_id = d.Dept_id ;

select * from Employee e natural join Dept d where (d.Dept_name = "IT" or d.Dept_name = "HR") and (e.Emp_fname like "A%" or e.Emp_fname like "O%");

update employee set Emp_salary = 0.10 * Emp_salary where year(Emp_JoinDate)<2020;

select p.Proj_id, p.Proj_name from project p natural join dept d where d.Dept_location="Pune";

 select p.Proj_Name , p.Proj_cost from project p where Proj_cost between 10000 and 50000000;

 select Proj_name from project p where Proj_cost = (select max(Proj_cost) from project);

select Emp_id, Emp_fname , Emp_lname from employee order by Emp_lname;

SELECT s.s_name
FROM Student s
JOIN Training t ON s.T_id = t.T_id
WHERE t.T_year IN (2019, 2021)
GROUP BY s.s_name
HAVING COUNT(DISTINCT t.T_year) = 2;

