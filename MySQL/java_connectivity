import java.sql.*;

public class sql {
    public static void main(String[] args) {
        try {
            // Load MySQL JDBC driver
            Class.forName("com.mysql.cj.jdbc.Driver");

            // Connect to MySQL (adjust DB name, user, password as needed)
            Connection conn = DriverManager.getConnection("jdbc:mysql://localhost:3306/test", "root", "123456");

            // Insert one row
            Statement stmt = conn.createStatement();
            stmt.executeUpdate("INSERT INTO students (name, roll) VALUES ('Obaidullah', 101)");
            stmt.executeUpdate("INSERT INTO students (name, roll) VALUES ('Zaid', 102)");

            // Update the row
            stmt.executeUpdate("UPDATE students SET roll = 999 WHERE name = 'Obaidullah'");

            // Delete the row
            //stmt.executeUpdate("DELETE FROM students WHERE name = 'Obaidullah'");

            // Read and print all rows
            ResultSet rs = stmt.executeQuery("SELECT * FROM students");
            while (rs.next())
                System.out.println(rs.getString(1) + " " + rs.getInt(2));

            conn.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

//javac -cp ".;mysql-connector-java-8.0.22.jar" sql.java  [for linux .:]

//java -cp ".;mysql-connector-java-8.0.22.jar" sql  [for linux .:]

//https://drive.google.com/drive/folders/1mIsOLFsFIem9q-iboPt004cl-Qtb1VrU
